---
title: Reference
description: A nested reference folder that should appear as an expandable sidebar item.
icon: lucide:key-round
---

This page proves that a nested docs folder can have its own index page and child pages.

## Standard nested folder

The folder appears in the sidebar and expands when a child route is active. The route for this page is `/docs/getting-started/reference`.

## Child pages

The child route `/docs/getting-started/reference/api-keys` should appear beneath this item and inherit the active section tab.

::read-more{to="$docs/getting-started/reference/api-keys" title="API Keys" icon="lucide:key-round"}
::
