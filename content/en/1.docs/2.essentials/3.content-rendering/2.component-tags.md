---
title: Component Tags
description: Exercise component-rich Markdown rendered by Ginko Content.
icon: lucide:blocks
badge: Optional
---

Comark-compatible component tags let docs authors use reusable Vue components inside Markdown. Generic tags come from `@lupinum/content-components`; business tags stay in the starter.

## Callouts

::note{title="Note"}
Use notes for neutral context that should stay close to the surrounding prose.
::

::info{title="Info"}
Use info callouts for helpful details, background, and non-blocking implementation hints.
::

::warning{title="Warning"}
Use warnings when content can be valid but needs attention before it ships.
::

::error{title="Error"}
Use errors for broken states, invalid configuration, or steps that must be corrected.
::

::success{title="Success"}
Use success callouts for completed checks, accepted output, or confirmed setup.
::

::idea{title="Idea"}
Use ideas for optional improvements, alternatives, or follow-up refinements.
::

::alert{title="Alert alias" type="info"}
The `alert` tag wraps the same callout system and keeps existing Markdown compatible.
::

::callout{title="Configurable callout" type="warning" icon="lucide:sparkles"}
The base `callout` tag remains available when authors need to choose the type or icon directly.
::

## Layout

:::layout
::column
Columns stack on mobile and become two balanced columns from medium screens upward.

Use normal prose, lists, images, or any other content component inside each column.
::
::column
::figure{src="/images/content-demo-detail.png" alt="Editorial image placeholder" width="1200" height="800" aspect="video"}
::
:::

::center{size="md" type="outline"}
Centered prose blocks use the same `default`, `border`, `border-dashed`, `outline`, and `outline-dashed` style vocabulary.
::

## Cards

::card-group{cols=2}
::card{title="Collection config" description="Schemas and routes live in one config file." icon="lucide:file-code" iconColor="primary" to="$docs/essentials/navigation"}
::
::card{title="Static output" description="Generated HTML must include docs and blog routes." icon="lucide:globe" iconColor="info" to="$blog/prerender-seo-search"}
::
::

Use `iconColor` to color card icons with design tokens. Supported values are `muted`, `foreground`, `primary`, `info`, `success`, `warning`, and `destructive`.

## Code Groups

::code-group

```bash [pnpm]
pnpm add @nuxt/ui
```

```bash [yarn]
yarn add @nuxt/ui
```

```bash [npm]
npm install @nuxt/ui
```

```bash [bun]
bun add @nuxt/ui
```

::

## Tabs

::tabs
::tab{label="Module" icon="package"}
The package registers generic prose and MDC components.
::
::tab{label="Starter" icon="rocket"}
The starter keeps project-specific business and legal components.
::
::

## Images

Regular Markdown images use the prose image component and stay inside the reading flow.

::doc-img
![Bright workspace placeholder](/images/content-demo-workspace.jpg)
::

For important page sections, an image can gently bleed beyond the text measure on larger screens.

::figure{src="/images/content-demo-workspace.jpg" alt="Bright workspace placeholder" caption="Bleed images stay aligned on mobile and only break out from medium viewports upward." width="1600" height="900" bleed=true aspect="video"}
::

### Image Rows

::gallery{layout="rows" targetRowHeight=220 spacing=8 bleed=true caption="Rows layout with Nuxt Photo for editorial image sequences."}
::figure{src="/images/content-demo-workspace.jpg" alt="Workspace placeholder" width="1600" height="900"}
::
::figure{src="/images/content-demo-workshop.jpg" alt="Workshop placeholder" width="1200" height="900"}
::
::figure{src="/images/content-demo-detail.png" alt="Detail placeholder" width="1200" height="800"}
::
::

### Image Grids

::gallery{layout="columns" columns=3 spacing=10 caption="Columns layout for balanced project or team grids."}
::figure{src="/images/content-demo-workshop.jpg" alt="Workshop placeholder" width="1200" height="900"}
::
::figure{src="/images/content-demo-detail.png" alt="Detail placeholder" width="1200" height="800"}
::
::figure{src="/images/content-demo-portrait.webp" alt="Portrait placeholder" width="900" height="1200"}
::
::

::gallery{layout="masonry" columns=3 spacing=10 caption="Masonry layout for mixed formats without cropping everything to one height."}
::figure{src="/images/content-demo-workspace.jpg" alt="Workspace placeholder" width="1600" height="900"}
::
::figure{src="/images/content-demo-portrait.webp" alt="Portrait placeholder" width="900" height="1200"}
::
::figure{src="/images/content-demo-workshop.jpg" alt="Workshop placeholder" width="1200" height="900"}
::
::figure{src="/images/content-demo-detail.png" alt="Detail placeholder" width="1200" height="800"}
::
::

## Steps

::steps
:::step{title="Define" icon="file-plus"}
Create the collection in `content.config.ts`.
:::

:::step{title="Render" icon="panel-top"}
Load the route page with `useContentPage()` and render it with `<ContentRenderer>`.
:::

:::step{title="Verify" icon="check-circle-2"}
Use generated output checks and browser inspection before calling the migration complete.
:::
::

## Linked references

::read-more{to="$docs/getting-started/reference" title="Nested reference folder" icon="lucide:key-round"}
::

## App-owned components

The chart component owns its demo data. The agent Markdown renderer reads the same source and emits it as an XML block with a JSON payload.

::chart
::
