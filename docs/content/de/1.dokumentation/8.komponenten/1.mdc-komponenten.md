---
title: MDC-Komponenten
description: Alle Markdown-Komponenten auf einer Seite, mit Live-Beispielen, kopierbarem Code, Eigenschaften und Standardwerten.
navigation:
  title: MDC-Komponenten
  icon: lucide:blocks
---

Wähle eine Komponente, probiere das Beispiel aus und kopiere das Markdown in deine Seite. Diese Referenz enthält alle 34 eingebauten Tags, einschließlich normaler Bilder. Ersetze die Beispieltexte, Links und Bildpfade durch deine eigenen Inhalte.

MDC bedeutet Markdown Components. Damit ergänzt du Markdown um strukturierte Blöcke. Eine **Eigenschaft** (auch **Prop**) verändert eine Komponente, etwa ihren Titel. Ein **Slot** ist der Inhalt zwischen der öffnenden und der schließenden Zeile.

## Hier beginnen

- Schreibe Komponentennamen mit großem Anfangsbuchstaben: `<Note title="Bevor du beginnst">…</Note>`.
- Schreibe jeden öffnenden Tag in eine Zeile. Das Markdown dazwischen kann mehrere Zeilen umfassen. Die Syntax bleibt bei Verschachtelung gleich.
- Setze Textattribute in Anführungszeichen. Verwende für typisierte Werte statisches JSON: `:zoom="false"` oder `:defaultValue='["first", "second"]'`. Ein boolesches Attribut ohne Wert wie `collapsible` bedeutet `true`.
- Verwende `<Figure src="/images/photo.webp" alt="Beschreibung" />` für Komponenten ohne Inhalt und `<Kbd>Enter</Kbd>` innerhalb eines Satzes.
- Schreibe benannte Slots als `<template #title>…</template>` innerhalb ihrer Komponente.
- Eigenschaften sind optional, sofern sie nicht als **erforderlich** markiert sind. Namen und Werte bleiben auch auf deutschen Seiten Englisch.
- Ginko Content liest diese Tags mit Comark. Werte müssen statisch sein; JavaScript-Ausdrücke, Events, Direktiven und beliebige Styles sind nicht erlaubt.

Links mit `$docs/` verweisen auf Seiten der Docs-Sammlung. Ersetze sie durch eine passende Seite deiner Website; siehe [Inhaltslinks]($docs/authoring/links). Die Bildbeispiele verwenden eine Datei aus dem Ordner `public/images/` dieser Website. Lege dort dein eigenes Bild ab, bevor du ein Bildbeispiel übernimmst.

Springe über das Inhaltsverzeichnis zur gewünschten Komponente. Zusammengehörige Tags wie `tabs` und `tab` stehen im selben Abschnitt.

## Darstellung

`appearance="quiet"` verwendet eine zurückhaltende Fläche. `appearance="tint"` ergänzt eine farbige Fläche. Die Eigenschaftstabellen zeigen, welche Komponenten diese Einstellung unterstützen. Ohne Angabe gilt zuerst die Website-Einstellung der Komponentenfamilie, dann die globale Einstellung. Der eingebaute Rückfallwert ist `quiet`. Eine Karte übernimmt zusätzlich die Darstellung ihres umgebenden `cards`-Blocks, sofern sie keine eigene Angabe hat.

Vergleiche beide Varianten in der [Komponentenübersicht]($docs/components/component-showcase).

## note

Ergänze einen hilfreichen Hinweis.

<Note title="Merken" appearance="tint">
Speichere deinen Entwurf, bevor du den Editor schließt.
</Note>

**In deine Markdown-Datei kopieren:**

```mdc [content/docs/component-example.md]
<Note title="Merken" appearance="tint">
Speichere deinen Entwurf, bevor du den Editor schließt.
</Note>
```

| Eigenschaft  | Werte, Standard und Wirkung                                           |
| ------------ | --------------------------------------------------------------------- |
| `title`      | Text. Standardmäßig kein Titel.                                       |
| `icon`       | Icon-Name wie `lucide:info`. Ohne Angabe erscheint das Icon für note. |
| `appearance` | `quiet` oder `tint`. Ohne Angabe gilt die Website-Einstellung.        |

Inhalt: Markdown im Standard-Slot.

## info

Erkläre ein Detail, das Leserinnen und Leser benötigen.

<Info title="Bildgröße" appearance="tint">
Das Bild passt sich an die verfügbare Breite an.
</Info>

**In deine Markdown-Datei kopieren:**

```mdc [content/docs/component-example.md]
<Info title="Bildgröße" appearance="tint">
Das Bild passt sich an die verfügbare Breite an.
</Info>
```

| Eigenschaft  | Werte, Standard und Wirkung                                           |
| ------------ | --------------------------------------------------------------------- |
| `title`      | Text. Standardmäßig kein Titel.                                       |
| `icon`       | Icon-Name wie `lucide:info`. Ohne Angabe erscheint das Icon für info. |
| `appearance` | `quiet` oder `tint`. Ohne Angabe gilt die Website-Einstellung.        |

Inhalt: Markdown im Standard-Slot.

## success

Bestätige ein abgeschlossenes Ergebnis.

<Success title="Fertig" appearance="tint">
Dein Text ist bereit zur Durchsicht.
</Success>

**In deine Markdown-Datei kopieren:**

```mdc [content/docs/component-example.md]
<Success title="Fertig" appearance="tint">
Dein Text ist bereit zur Durchsicht.
</Success>
```

| Eigenschaft  | Werte, Standard und Wirkung                                              |
| ------------ | ------------------------------------------------------------------------ |
| `title`      | Text. Standardmäßig kein Titel.                                          |
| `icon`       | Icon-Name wie `lucide:info`. Ohne Angabe erscheint das Icon für success. |
| `appearance` | `quiet` oder `tint`. Ohne Angabe gilt die Website-Einstellung.           |

Inhalt: Markdown im Standard-Slot.

## warning

Weise vor einer Handlung auf einen wahrscheinlichen Fehler hin.

<Warning title="Vor dem Veröffentlichen" appearance="tint">
Prüfe die Termine, bevor du die Veranstaltung veröffentlichst.
</Warning>

**In deine Markdown-Datei kopieren:**

```mdc [content/docs/component-example.md]
<Warning title="Vor dem Veröffentlichen" appearance="tint">
Prüfe die Termine, bevor du die Veranstaltung veröffentlichst.
</Warning>
```

| Eigenschaft  | Werte, Standard und Wirkung                                              |
| ------------ | ------------------------------------------------------------------------ |
| `title`      | Text. Standardmäßig kein Titel.                                          |
| `icon`       | Icon-Name wie `lucide:info`. Ohne Angabe erscheint das Icon für warning. |
| `appearance` | `quiet` oder `tint`. Ohne Angabe gilt die Website-Einstellung.           |

Inhalt: Markdown im Standard-Slot.

## error

Beschreibe ein Problem und seine Lösung.

<Error title="Bild fehlt" appearance="tint">
Füge die Bilddatei hinzu und prüfe anschließend ihren Pfad.
</Error>

**In deine Markdown-Datei kopieren:**

```mdc [content/docs/component-example.md]
<Error title="Bild fehlt" appearance="tint">
Füge die Bilddatei hinzu und prüfe anschließend ihren Pfad.
</Error>
```

| Eigenschaft  | Werte, Standard und Wirkung                                            |
| ------------ | ---------------------------------------------------------------------- |
| `title`      | Text. Standardmäßig kein Titel.                                        |
| `icon`       | Icon-Name wie `lucide:info`. Ohne Angabe erscheint das Icon für error. |
| `appearance` | `quiet` oder `tint`. Ohne Angabe gilt die Website-Einstellung.         |

Inhalt: Markdown im Standard-Slot.

## idea

Schlage eine mögliche Vorgehensweise vor.

<Idea title="Vergleich ausprobieren" appearance="tint">
Vergleiche die verfügbaren Möglichkeiten mit zwei Karten.
</Idea>

**In deine Markdown-Datei kopieren:**

```mdc [content/docs/component-example.md]
<Idea title="Vergleich ausprobieren" appearance="tint">
Vergleiche die verfügbaren Möglichkeiten mit zwei Karten.
</Idea>
```

| Eigenschaft  | Werte, Standard und Wirkung                                           |
| ------------ | --------------------------------------------------------------------- |
| `title`      | Text. Standardmäßig kein Titel.                                       |
| `icon`       | Icon-Name wie `lucide:info`. Ohne Angabe erscheint das Icon für idea. |
| `appearance` | `quiet` oder `tint`. Ohne Angabe gilt die Website-Einstellung.        |

Inhalt: Markdown im Standard-Slot.

## aside

Ergänze Hintergrundinformationen zum Haupttext.

<Aside label="Hintergrund">
Diese Anleitung richtet sich an das Redaktionsteam.
</Aside>

**In deine Markdown-Datei kopieren:**

```mdc [content/docs/component-example.md]
<Aside label="Hintergrund">
Diese Anleitung richtet sich an das Redaktionsteam.
</Aside>
```

| Eigenschaft  | Werte, Standard und Wirkung                                    |
| ------------ | -------------------------------------------------------------- |
| `label`      | Text über dem Inhalt. Standardmäßig keine Beschriftung.        |
| `appearance` | `quiet` oder `tint`. Ohne Angabe gilt die Website-Einstellung. |

Inhalt: Markdown im Standard-Slot.

## excerpt

Zitiere einen Abschnitt mit Quellenangabe.

<Excerpt label="Aus dem Projektbriefing" source="Redaktionsteam">
Mache die nächste Handlung deutlich.
</Excerpt>

**In deine Markdown-Datei kopieren:**

```mdc [content/docs/component-example.md]
<Excerpt label="Aus dem Projektbriefing" source="Redaktionsteam">
Mache die nächste Handlung deutlich.
</Excerpt>
```

| Eigenschaft  | Werte, Standard und Wirkung                                    |
| ------------ | -------------------------------------------------------------- |
| `label`      | Text über dem Zitat. Standardmäßig keine Beschriftung.         |
| `size`       | `body` (Standard), `display`: großer Text ohne Kasten.         |
| `source`     | Quellenangabe unter dem Zitat. Standardmäßig keine Quelle.     |
| `appearance` | `quiet` oder `tint`. Ohne Angabe gilt die Website-Einstellung. |

Inhalt: zitiertes Markdown im Standard-Slot.

## dropcap

Beginne einen längeren Abschnitt mit einem großen Anfangsbuchstaben.

<Dropcap lines="3">
Gute Geschichten beginnen mit einer klaren Idee. Beschreibe die Menschen, den Ort und das Ziel deines Projekts, bevor du auf Details eingehst. Gib deinen Leserinnen und Lesern genug Kontext, um die Bedeutung der Arbeit zu verstehen.
</Dropcap>

**In deine Markdown-Datei kopieren:**

```mdc [content/docs/component-example.md]
<Dropcap lines="3">
Gute Geschichten beginnen mit einer klaren Idee. Beschreibe die Menschen, den Ort und das Ziel deines Projekts, bevor du auf Details eingehst. Gib deinen Leserinnen und Lesern genug Kontext, um die Bedeutung der Arbeit zu verstehen.
</Dropcap>
```

| Eigenschaft | Werte, Standard und Wirkung              |
| ----------- | ---------------------------------------- |
| `lines`     | `2`, `3` oder `4`. Standard: `2` Zeilen. |

Inhalt: Absätze im Standard-Slot. Der erste Buchstabe des ersten Absatzes wird vergrößert.

## kbd

Zeige eine Taste innerhalb eines Satzes.

Drücke <Kbd>Enter</Kbd>, um fortzufahren.

**In deine Markdown-Datei kopieren:**

```mdc [content/docs/component-example.md]
Drücke <Kbd>Enter</Kbd>, um fortzufahren.
```

Keine Eigenschaften. Schreibe die Tastenbeschriftung in die eckigen Klammern.

## accordion und accordion-item

Verberge optionale Details hinter einer Frage. Wähle die Frage, um sie zu öffnen oder zu schließen.

<Accordion type="single" collapsible defaultValue="preparation">
<AccordionItem value="preparation" title="Was soll ich vorbereiten?">
Bereite den Text und ein Bild mit Alternativtext vor.
</AccordionItem>

<AccordionItem value="review" title="Wer prüft die Seite?">
Bitte eine weitere Person aus dem Redaktionsteam darum.
</AccordionItem>
</Accordion>

**In deine Markdown-Datei kopieren:**

```mdc [content/docs/component-example.md]
<Accordion type="single" collapsible defaultValue="preparation">
<AccordionItem value="preparation" title="Was soll ich vorbereiten?">
Bereite den Text und ein Bild mit Alternativtext vor.
</AccordionItem>

<AccordionItem value="review" title="Wer prüft die Seite?">
Bitte eine weitere Person aus dem Redaktionsteam darum.
</AccordionItem>
</Accordion>
```

| Eigenschaft              | Werte, Standard und Wirkung                                                                                              |
| ------------------------ | ------------------------------------------------------------------------------------------------------------------------ |
| `accordion.type`         | `single` (Standard) öffnet einen Eintrag; `multiple` erlaubt mehrere.                                                    |
| `accordion.collapsible`  | Wahrheitswert. Standard: `true`. Erlaubt im Modus single, den offenen Eintrag zu schließen.                              |
| `accordion.defaultValue` | Anfangs offener Eintrag; standardmäßig keiner. Bei `single` ein Textwert, bei `multiple` eine JSON-Liste von Textwerten. |
| `accordion.appearance`   | `quiet` oder `tint`. Ohne Angabe gilt die Website-Einstellung.                                                           |
| `accordion-item.value`   | Eindeutige Textkennung. Wird ohne Angabe erzeugt. Für `defaultValue` selbst festlegen.                                   |
| `accordion-item.title`   | Frage oder Überschrift. Standardmäßig kein Titel.                                                                        |
| `accordion-item.content` | Einfacher Inhaltstext. Standardmäßig leer; der Standard-Slot mit Markdown folgt danach.                                  |

Setze nur `accordion-item`-Kinder in `accordion`. Jeder Eintrag erlaubt Markdown im Standard-Slot, einen `<template #title>`-Slot statt der title-Eigenschaft und einen `<template #content>`-Slot statt der content-Eigenschaft.

## tabs und tab

Lass Leserinnen und Leser zwischen Alternativen wechseln. Probiere die Tabs mit den Pfeiltasten aus.

<Tabs layout="line">
<Tab label="Schreiben" icon="lucide:file-pen-line">
Beginne mit der Hauptaussage.
</Tab>

<Tab label="Prüfen" icon="lucide:check">
Prüfe den Text und die Links.
</Tab>
</Tabs>

**In deine Markdown-Datei kopieren:**

```mdc [content/docs/component-example.md]
<Tabs layout="line">
<Tab label="Schreiben" icon="lucide:file-pen-line">
Beginne mit der Hauptaussage.
</Tab>

<Tab label="Prüfen" icon="lucide:check">
Prüfe den Text und die Links.
</Tab>
</Tabs>
```

| Eigenschaft       | Werte, Standard und Wirkung                                                  |
| ----------------- | ---------------------------------------------------------------------------- |
| `tabs.layout`     | `separate` (Standard) oder `line`. Verändert die Darstellung der Tab-Leiste. |
| `tabs.padded`     | Wahrheitswert. Standard: `true`. Ergänzt Abstand um den Inhalt.              |
| `tabs.appearance` | `quiet` oder `tint`. Ohne Angabe gilt die Website-Einstellung.               |
| `tab.label`       | Tab-Beschriftung. Standard: `Tab 1`, `Tab 2`, in Reihenfolge.                |
| `tab.icon`        | Icon-Name. Standardmäßig kein Icon.                                          |

Setze nur `tab`-Kinder in `tabs`. Schreibe Markdown in jeden Tab. Der erste Tab ist anfangs geöffnet.

## cards und card

Biete eine kleine Auswahl an Möglichkeiten. Eine Karte mit `to` wird zum Link.

<Cards cols="2">
<Card title="Seite schreiben" description="Beginne mit deinem Text." icon="lucide:file-pen-line" to="$docs/authoring/write-content" footer="Anleitung öffnen">
Gliedere das Thema mit Überschriften.
</Card>

<Card title="Links ergänzen" description="Verbinde hilfreiche Seiten." icon="lucide:link" to="$docs/authoring/links">
Beschrifte jeden Link eindeutig.
</Card>
</Cards>

**In deine Markdown-Datei kopieren:**

```mdc [content/docs/component-example.md]
<Cards cols="2">
<Card title="Seite schreiben" description="Beginne mit deinem Text." icon="lucide:file-pen-line" to="$docs/authoring/write-content" footer="Anleitung öffnen">
Gliedere das Thema mit Überschriften.
</Card>

<Card title="Links ergänzen" description="Verbinde hilfreiche Seiten." icon="lucide:link" to="$docs/authoring/links">
Beschrifte jeden Link eindeutig.
</Card>
</Cards>
```

| Eigenschaft         | Werte, Standard und Wirkung                                                                                |
| ------------------- | ---------------------------------------------------------------------------------------------------------- |
| `cards.cols`        | `1`, `2` (Standard) oder `3`. Maximale Spaltenzahl; auf schmalen Bildschirmen stehen Karten untereinander. |
| `cards.appearance`  | `quiet` oder `tint`. Ohne Angabe gilt die Website-Einstellung.                                             |
| `card.title`        | Überschrift. Standardmäßig kein Titel.                                                                     |
| `card.description`  | Kurzer Text unter dem Titel. Standardmäßig keine Beschreibung.                                             |
| `card.footer`       | Text im Fußbereich. Standardmäßig kein Fußbereich.                                                         |
| `card.to`           | Seitenreferenz, Website-Pfad oder externe URL. Standardmäßig kein Link.                                    |
| `card.target`       | Linkziel, etwa `_blank` für einen neuen Tab. Standard: aktueller Tab.                                      |
| `card.icon`         | Icon-Name. Standardmäßig kein Icon.                                                                        |
| `card.iconColor`    | `muted` (Rückfallwert), `foreground`, `primary`, `info`, `success`, `warning` oder `destructive`.          |
| `card.img`          | Bildreferenz. Standardmäßig kein Bild. Dekorativ: Karten haben keine Eigenschaft für Bild-Alternativtext.  |
| `card.showLinkIcon` | Wahrheitswert. Standard: `true`. Zeigt den ausgehenden Pfeil bei externen Links.                           |
| `card.horizontal`   | Wahrheitswert. Standard: `false`. Platziert das Icon der Kopfzeile neben Titel und Beschreibung.           |
| `card.appearance`   | `quiet` oder `tint`. Ohne Angabe gilt die Website-Einstellung.                                             |

Verwende `card` einzeln oder innerhalb von `cards`. Der Standard-Slot enthält Markdown. Die benannten Slots `<template #title>`, `<template #description>` und `<template #footer>` ersetzen die jeweiligen Texteigenschaften. Setze keinen weiteren Link oder interaktiven Schalter in eine verlinkte Karte.

Für einen formatierten Fußbereich schreibst du die Slot-Markierung auf eine eigene Zeile:

```mdc [content/docs/component-example.md]
<Card title="Checkliste der Redaktion">
Lies den Entwurf laut vor.

<template #footer>
**Bereit zur Durchsicht**
</template>
</Card>
```

## read-more

Liste weiterführende Seiten mit optionalen Beschreibungen auf.

<ReadMore title="Anleitungen zum Schreiben" :links='[{"title":"Inhalte schreiben","to":"$docs/authoring/write-content","description":"Gliedere deine Seite.","icon":"lucide:file-pen-line"},{"title":"Links ergänzen","to":"$docs/authoring/links"}]' />

**In deine Markdown-Datei kopieren:**

```mdc [content/docs/component-example.md]
<ReadMore title="Anleitungen zum Schreiben" :links='[{"title":"Inhalte schreiben","to":"$docs/authoring/write-content","description":"Gliedere deine Seite.","icon":"lucide:file-pen-line"},{"title":"Links ergänzen","to":"$docs/authoring/links"}]' />
```

| Eigenschaft  | Werte, Standard und Wirkung                                                           |
| ------------ | ------------------------------------------------------------------------------------- |
| `title`      | Überschrift. Standardmäßig kein Titel.                                                |
| `links`      | **Erforderlich.** JSON-Liste. Jeder Eintrag benötigt die Textfelder `title` und `to`. |
| `appearance` | `quiet` oder `tint`. Ohne Angabe gilt die Website-Einstellung.                        |

Jeder Link erlaubt zusätzlich `description`, `icon` und `target` (Text). Diese Angaben fehlen standardmäßig; Links öffnen im aktuellen Tab, sofern `target` das nicht ändert. Keine Slots. Eine leere Liste zeigt nichts an.

## flow

Begrenzt Artikeltext auf eine mittige Lesespalte. Eine direkte `figure` mit `bleed="outside"` nutzt die gesamte verfügbare Inhaltsbreite, ohne in die Navigation zu ragen.

<Flow>
Ein kurzer Abschnitt bleibt auf einer breiten Seite lesbar.
</Flow>

```mdc [content/docs/artikel.md]
<Flow surface="tint">
Dein Artikeltext.
</Flow>
```

| Eigenschaft | Werte, Standard und Wirkung                                                                                                     |
| ----------- | ------------------------------------------------------------------------------------------------------------------------------- |
| `width`     | `reading` (Standard): höchstens 65 Zeichenbreiten. `wide`: gesamte verfügbare Breite. Text bleibt am Zeilenanfang ausgerichtet. |
| `surface`   | `default` (Standard), `muted`, `tint`. Ein Hintergrund ergänzt Innenabstand.                                                    |

Inhalt: Markdown und Komponenten im Standard-Slot. Nutze `layout` für Spalten und `figure.placement` für Textumfluss. Überschriften, Listen, Tabellen, Codeblöcke und andere Blockkomponenten beginnen unter einem umflossenen Bild. Die Hintergrundfläche bleibt im Inhaltsbereich, auch auf Seiten mit seitlicher Navigation.

Die [Magazin-Layouts]($docs/components/magazine-layouts) enthalten vollständige Kompositionen. Schreibe in der Winkelsyntax `<Figure>` mit großem F, um die Komponente vom HTML-Element `<figure>` zu unterscheiden.

## layout und column

Platziere zusammengehörige Inhalte in Spalten. Auf schmalen Bildschirmen stehen sie untereinander.

<Layout type="border">
<Column size="sm">
**Vorbereiten**

Sammle Texte und Bilder.
</Column>

<Column size="lg">
**Prüfen**

Prüft die Inhalte vor dem Veröffentlichen gemeinsam.
</Column>
</Layout>

**In deine Markdown-Datei kopieren:**

```mdc [content/docs/component-example.md]
<Layout type="border">
<Column size="sm">
**Vorbereiten**

Sammle Texte und Bilder.
</Column>

<Column size="lg">
**Prüfen**

Prüft die Inhalte vor dem Veröffentlichen gemeinsam.
</Column>
</Layout>
```

| Eigenschaft      | Werte, Standard und Wirkung                                                               |
| ---------------- | ----------------------------------------------------------------------------------------- |
| `layout.type`    | `default` (Standard), `card`, `border`, `border-dashed`, `outline` oder `outline-dashed`. |
| `column.size`    | `xs`, `sm`, `md` (Standard), `lg` oder `xl`. Legt die relative Spaltenbreite fest.        |
| `layout.align`   | `start`, `center`, `end`.                                                                 |
| `layout.gap`     | `none`, `sm`, `md`, `lg`.                                                                 |
| `layout.stack`   | `sm` (32 rem), `md` (40 rem), `lg` (48 rem).                                              |
| `layout.surface` | `default`, `muted`, `tint`.                                                               |
| `column.align`   | `start`, `center`, `end`.                                                                 |
| `column.media`   | `natural`, `cover`, `contain`.                                                            |

Setze `column`-Kinder in `layout` und Markdown in jede Spalte. Diese Tags haben keine Eigenschaft `appearance`.

Ausrichtung, Bildfüllung, Abstände und Hintergründe findest du unter [Editoriale Layouts]($docs/components/editorial-layouts).

## center

Zentriere einen kurzen Abschnitt mit begrenzter Breite.

<Center size="lg" type="border">
**Ein gemeinsames Ziel**

Mache die Seite für ihre Leserinnen und Leser nützlich.
</Center>

**In deine Markdown-Datei kopieren:**

```mdc [content/docs/component-example.md]
<Center size="lg" type="border">
**Ein gemeinsames Ziel**

Mache die Seite für ihre Leserinnen und Leser nützlich.
</Center>
```

| Eigenschaft | Werte, Standard und Wirkung                                                                                        |
| ----------- | ------------------------------------------------------------------------------------------------------------------ |
| `size`      | `xs`, `sm`, `md` (Standard), `lg` oder `xl`. Maximale Inhaltsbreite.                                               |
| `max`       | `sm`, `md` oder `lg`. Überschreibt optional `size`; standardmäßig nicht gesetzt. Verwende für neue Inhalte `size`. |
| `type`      | `default` (Standard), `card`, `border`, `border-dashed`, `outline` oder `outline-dashed`.                          |

Inhalt: Markdown im Standard-Slot. Keine Eigenschaft `appearance`.

## steps

Zeige eine Folge von Anweisungen. Jede Überschrift der dritten Ebene beginnt einen Schritt.

<Steps mode="numbered">
### Entwurf vorbereiten

Schreibe den Text und ergänze ein Bild.

### Entwurf prüfen

Prüfe Rechtschreibung, Links und Alternativtext.
</Steps>

**In deine Markdown-Datei kopieren:**

```mdc [content/docs/component-example.md]
<Steps mode="numbered">
### Entwurf vorbereiten

Schreibe den Text und ergänze ein Bild.

### Entwurf prüfen

Prüfe Rechtschreibung, Links und Alternativtext.
</Steps>
```

| Eigenschaft  | Werte, Standard und Wirkung                                                     |
| ------------ | ------------------------------------------------------------------------------- |
| `mode`       | `icons` (Standard) verwendet Häkchen. `numbered` verwendet fortlaufende Zahlen. |
| `appearance` | `quiet` oder `tint`. Ohne Angabe gilt die Website-Einstellung.                  |

Inhalt: `###`-Überschriften, gefolgt von Markdown. Schreibe die Einleitung vor die Komponente; Inhalt vor der ersten Überschrift bildet keinen Schritt.

## timeline und timeline-item

Zeige Meilensteine mit Datum. Datumsangaben sind Text; die Einträge bleiben in deiner Reihenfolge.

<Timeline>
<TimelineItem date="10. September" title="Entwurf" icon="lucide:file-pen-line">
Die erste Version ist fertig.
</TimelineItem>

<TimelineItem date="12. September" label="Aktueller Stand" title="Durchsicht" active>
Das Team prüft den Text.
</TimelineItem>
</Timeline>

**In deine Markdown-Datei kopieren:**

```mdc [content/docs/component-example.md]
<Timeline>
<TimelineItem date="10. September" title="Entwurf" icon="lucide:file-pen-line">
Die erste Version ist fertig.
</TimelineItem>

<TimelineItem date="12. September" label="Aktueller Stand" title="Durchsicht" active>
Das Team prüft den Text.
</TimelineItem>
</Timeline>
```

| Eigenschaft            | Werte, Standard und Wirkung                                                                              |
| ---------------------- | -------------------------------------------------------------------------------------------------------- |
| `timeline.appearance`  | `quiet` oder `tint`. Ohne Angabe gilt die Website-Einstellung.                                           |
| `timeline-item.date`   | Datumstext. Standardmäßig kein Datum. Setze ISO-Daten in Anführungszeichen, etwa `"2026-09-10"`. |
| `timeline-item.label`  | Kurzer Statustext. Standardmäßig keine Beschriftung.                                                     |
| `timeline-item.title`  | Überschrift des Meilensteins. Standardmäßig kein Titel.                                                  |
| `timeline-item.icon`   | Icon-Name. Standard: ein Punkt.                                                                          |
| `timeline-item.active` | Wahrheitswert. Standard: `false`. Hebt den Eintrag hervor.                                               |

Setze `timeline-item`-Kinder in `timeline`. Jeder Eintrag erlaubt Markdown im Standard-Slot.

## code-group

Lass Leserinnen und Leser zwischen Codebeispielen wechseln. Beschrifte jeden Codeblock mit einem Dateinamen.

<CodeGroup>
```json [entwurf.json]
{ "status": "Entwurf" }
```

```yaml [entwurf.yml]
status: Entwurf
```
</CodeGroup>

**In deine Markdown-Datei kopieren:**

~~~mdc [content/docs/component-example.md]
<CodeGroup>
```json [entwurf.json]
{ "status": "Entwurf" }
```

```yaml [entwurf.yml]
status: Entwurf
```
</CodeGroup>
~~~

| Eigenschaft  | Werte, Standard und Wirkung                                    |
| ------------ | -------------------------------------------------------------- |
| `appearance` | `quiet` oder `tint`. Ohne Angabe gilt die Website-Einstellung. |

Inhalt: eingezäunte Codeblöcke. Anfangs ist der erste Block ausgewählt. Codeblöcke erlauben eine Sprache, `[Dateiname]` und Zeilenmarkierungen wie `{1,3-5}`.

## code-tree

Durchsuche mehrere Dateien. Wähle einen Dateinamen, um seinen Code zu sehen.

<CodeTree defaultValue="content/draft.yml" expandAll>
```yaml [content/draft.yml]
title: Eine neue Geschichte
status: Entwurf
```

```json [site.json]
{ "name": "Unser Projekt" }
```
</CodeTree>

**In deine Markdown-Datei kopieren:**

~~~mdc [content/docs/component-example.md]
<CodeTree defaultValue="content/draft.yml" expandAll>
```yaml [content/draft.yml]
title: Eine neue Geschichte
status: Entwurf
```

```json [site.json]
{ "name": "Unser Projekt" }
```
</CodeTree>
~~~

| Eigenschaft    | Werte, Standard und Wirkung                                                                                         |
| -------------- | ------------------------------------------------------------------------------------------------------------------- |
| `defaultValue` | Anfangs ausgewählter Dateipfad. Standard: erste Datei. Muss genau mit einem Dateinamen am Codeblock übereinstimmen. |
| `expandAll`    | Wahrheitswert. Standard: `false`. Öffnet alle Ordner; sonst nur die übergeordneten Ordner der ausgewählten Datei.   |
| `appearance`   | `quiet` oder `tint`. Ohne Angabe gilt die Website-Einstellung.                                                      |

Inhalt: mindestens ein beschrifteter Codeblock. Verwende eindeutige relative Pfade mit `/` als Trennzeichen. Leere Pfadteile, `.` und `..` sind ungültig. Verwende einen Dateipfad nicht zugleich als Ordner für eine andere Datei.

## collapse

Kürze einen langen Block, bis die Leserin oder der Leser ihn aufklappt. Probiere den Schalter unten aus.

<Collapse>
```text [redaktions-checkliste.txt]
01 Thema bestätigen
02 Zielgruppe bestimmen
03 Hauptaussage schreiben
04 Einleitung entwerfen
05 Überschriften ergänzen
06 Kurze Absätze schreiben
07 Hilfreiches Beispiel ergänzen
08 Bild auswählen
09 Alternativtext schreiben
10 Bildunterschrift ergänzen
11 Bildrechte prüfen
12 Namen prüfen
13 Termine prüfen
14 Zahlen prüfen
15 Rechtschreibung prüfen
16 Jeden Link prüfen
17 Linktexte prüfen
18 Wiederholungen entfernen
19 Unbekannte Begriffe erklären
20 Entwurf laut lesen
21 Vorschau am Handy prüfen
22 Vorschau am Desktop prüfen
23 Um Durchsicht bitten
24 Rückmeldung einarbeiten
25 Endgültigen Entwurf speichern
```
</Collapse>

**In deine Markdown-Datei kopieren:**

~~~mdc [content/docs/component-example.md]
<Collapse>
```text [redaktions-checkliste.txt]
01 Thema bestätigen
02 Zielgruppe bestimmen
03 Hauptaussage schreiben
04 Einleitung entwerfen
05 Überschriften ergänzen
06 Kurze Absätze schreiben
07 Hilfreiches Beispiel ergänzen
08 Bild auswählen
09 Alternativtext schreiben
10 Bildunterschrift ergänzen
11 Bildrechte prüfen
12 Namen prüfen
13 Termine prüfen
14 Zahlen prüfen
15 Rechtschreibung prüfen
16 Jeden Link prüfen
17 Linktexte prüfen
18 Wiederholungen entfernen
19 Unbekannte Begriffe erklären
20 Entwurf laut lesen
21 Vorschau am Handy prüfen
22 Vorschau am Desktop prüfen
23 Um Durchsicht bitten
24 Rückmeldung einarbeiten
25 Endgültigen Entwurf speichern
```
</Collapse>
~~~

| Eigenschaft  | Werte, Standard und Wirkung                                    |
| ------------ | -------------------------------------------------------------- |
| `appearance` | `quiet` oder `tint`. Ohne Angabe gilt die Website-Einstellung. |

Inhalt: Markdown oder Code im Standard-Slot. Der Inhalt ist anfangs auf 480 px begrenzt. Der Schalter erscheint nur, wenn der Inhalt diese Höhe überschreitet. Aufklappen zeigt den gesamten Inhalt; es gibt keinen Schalter zum erneuten Einklappen und keine Höheneigenschaft.

## files

Zeige eine Ordnerstruktur ohne Dateiinhalte.

<Files active="content/story.md" :annotations='{"content/story.md":"Dein Artikel"}'>
- `content`
  - `story.md`
- `public`
  - `images`
    - `cover.jpg`
</Files>

**In deine Markdown-Datei kopieren:**

```mdc [content/docs/component-example.md]
<Files active="content/story.md" :annotations='{"content/story.md":"Dein Artikel"}'>
- `content`
  - `story.md`
- `public`
  - `images`
    - `cover.jpg`
</Files>
```

| Eigenschaft   | Werte, Standard und Wirkung                                                                       |
| ------------- | ------------------------------------------------------------------------------------------------- |
| `active`      | Hervorzuhebender Pfad, ohne führenden Schrägstrich. Standardmäßig keine aktive Datei.             |
| `annotations` | JSON-Objekt, das vollständige Pfade Textbeschriftungen zuordnet. Standardmäßig keine Anmerkungen. |
| `appearance`  | `quiet` oder `tint`. Ohne Angabe gilt die Website-Einstellung.                                    |

Inhalt: eine verschachtelte Markdown-Liste. Setze jeden Dateinamen in Backticks. Die Zeilen beschreiben Dateien; sie öffnen sie nicht.

## api

Beschreibe strukturierte Felder in einem Referenzbereich. Verwende für einen kurzen, einfachen Vergleich eine Tabelle.

<Api title="Artikelfelder" icon="lucide:file-text" :groups='[{"label":"Felder","entries":[{"name":"title","annotation":"string","required":true,"description":"Die Überschrift des Artikels."},{"name":"summary","annotation":"string","optional":true,"description":"Eine kurze Einleitung."}]}]' />

**In deine Markdown-Datei kopieren:**

```mdc [content/docs/component-example.md]
<Api title="Artikelfelder" icon="lucide:file-text" :groups='[{"label":"Felder","entries":[{"name":"title","annotation":"string","required":true,"description":"Die Überschrift des Artikels."},{"name":"summary","annotation":"string","optional":true,"description":"Eine kurze Einleitung."}]}]' />
```

| Eigenschaft  | Werte, Standard und Wirkung                                                                                                                                                              |
| ------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `title`      | Kopfzeilentext. Standardmäßig kein Titel.                                                                                                                                                |
| `icon`       | Icon-Name für die Kopfzeile. Standardmäßig kein Icon.                                                                                                                                    |
| `method`     | HTTP-Methode als Text, etwa `post`. Standardmäßig keine Methode.                                                                                                                         |
| `path`       | Endpunktpfad, etwa `/api/articles`. Standardmäßig kein Pfad.                                                                                                                             |
| `groups`     | **Für die Darstellung erforderlich.** Nicht leere JSON-Liste von Gruppen mit jeweils eindeutiger, nicht leerer `label`-Beschriftung und nicht leerer `entries`-Liste. Kein Standardwert. |
| `appearance` | `quiet` oder `tint`. Ohne Angabe gilt die Website-Einstellung.                                                                                                                           |

Der Inhalt gehört in `groups`. Der Standard-Slot ist laut Markdown-Policy erlaubt, wird aber nicht angezeigt. Verwende `title` für eine allgemeine Kopfzeile oder `method` mit `path` für einen Endpunkt.

Jedes Objekt in `entries` erlaubt diese Felder:

| Feld          | Werte, Standard und Wirkung                                                                                                                                    |
| ------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `name`        | **Erforderlich.** Nicht leerer Text, innerhalb der Gruppe eindeutig.                                                                                           |
| `annotation`  | Typ als Text, etwa `string`. Standardmäßig nicht gesetzt.                                                                                                      |
| `required`    | Wahrheitswert. Standard: `false`. Ergänzt eine Markierung für erforderliche Felder.                                                                            |
| `optional`    | Wahrheitswert. Standard: `false`. Ergänzt `?` am Namen. Nicht mit `required: true` kombinieren.                                                                |
| `deprecated`  | Wahrheitswert. Standard: `false`. Markiert ein veraltetes Feld.                                                                                                |
| `since`       | Versionsangabe als Text. Standardmäßig nicht gesetzt.                                                                                                          |
| `default`     | Text, der den Standardwert des Feldes beschreibt. Zahlen und Wahrheitswerte in Anführungszeichen setzen, etwa `default: "false"`. Standardmäßig nicht gesetzt. |
| `description` | Text unter der Signatur. Backticks formatieren Inline-Code. Standardmäßig nicht gesetzt.                                                                       |

Optionale Textfelder dürfen nicht leer sein, wenn du sie angibst. Ungültige Gruppen oder Einträge stoppen die Darstellung mit einer Fehlermeldung; sie werden nicht stillschweigend entfernt. Erzeugte Eintrags-IDs müssen eindeutig sein. Verwende über mehrere Bereiche hinweg unterschiedliche Kombinationen aus Gruppe und Name, damit keine doppelten Sprungmarken entstehen. Die [Anleitung zu API-Bereichen]($docs/components/api-reference) erklärt Kopfzeilenvarianten und Direktlinks zu Einträgen.

## img – normale Markdown-Bilder

Verwende Markdown-Bildsyntax für ein Bild ohne sichtbare Bildunterschrift. Der eingebaute Tag `img` stellt diese Syntax dar.

![Eine Stadtsilhouette bei Sonnenuntergang](/images/content-demo-workspace.jpg "Stadtsilhouette")

**In deine Markdown-Datei kopieren:**

```mdc [content/docs/component-example.md]
![Eine Stadtsilhouette bei Sonnenuntergang](/images/content-demo-workspace.jpg "Stadtsilhouette")
```

| Eigenschaft | Werte, Standard und Wirkung                                                                                                                               |
| ----------- | --------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `src`       | Bildpfad oder URL; steht in runden Klammern. Für die Bilddarstellung notwendig.                                                                           |
| `alt`       | Alternativtext; steht in eckigen Klammern. Beschreibe inhaltlich relevante Bilder; verwende leeren Text nur für dekorative Bilder. Standard: leerer Text. |
| `title`     | Optionaler Titel in Anführungszeichen nach dem Pfad. Standardmäßig kein Titel; keine sichtbare Bildunterschrift.                                          |
| `width`     | Optionale numerische Bildbreite. Standardmäßig keine explizite Breite.                                                                                    |
| `height`    | Optionale numerische Bildhöhe. Standardmäßig keine explizite Höhe.                                                                                        |

Keine Slots. Normale Bilder folgen der globalen Zoom-Einstellung der Website. Verwende `figure` für explizite Abmessungen, sichtbare Bildunterschriften oder Zoom-Einstellungen pro Bild.

## figure

Zeige ein Bild mit Bildunterschrift. Wähle das Bild, um den Zoom auszuprobieren; Escape schließt ihn.

<Figure src="/images/content-demo-workspace.jpg" alt="Hochhäuser unter einem Abendhimmel" caption="Die Stadt am Ende des Tages." aspect="video" fit="cover" zoom />

**In deine Markdown-Datei kopieren:**

```mdc [content/docs/component-example.md]
<Figure src="/images/content-demo-workspace.jpg" alt="Hochhäuser unter einem Abendhimmel" caption="Die Stadt am Ende des Tages." aspect="video" fit="cover" zoom />
```

| Eigenschaft  | Werte, Standard und Wirkung                                                                                                                                   |
| ------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `src`        | **Erforderlich.** Bildpfad oder URL.                                                                                                                          |
| `alt`        | **Erforderlich.** Alternativtext, der das Bild beschreibt.                                                                                                    |
| `caption`    | Text unter dem Bild. Ohne Angabe wird `alt` angezeigt.                                                                                                        |
| `width`      | Zahl oder numerischer Text. Explizite Bildbreite; standardmäßig nicht gesetzt.                                                                                |
| `height`     | Zahl oder numerischer Text. Explizite Bildhöhe; standardmäßig nicht gesetzt.                                                                                  |
| `bleed`      | `outside` erweitert das Bild über die Textspalte hinaus. Für normale Breite weglassen.                                                                        |
| `aspect`     | `auto` (Rückfallwert), `video` (16:9), `wide` (21:9), `square` (1:1) oder `portrait` (4:5).                                                                   |
| `placement`  | `inline` (Standard), `start`, `end`. Direkt in `flow` fließt Text ab 40 rem verfügbarer Breite um das Bild. Darunter bleibt die Dokumentreihenfolge erhalten. |
| `frame`      | `default` (Standard), `none`. `none` entfernt die Fläche und setzt die Bildunterschrift unter das Bild.                                                       |
| `focus`      | `center` (Standard), `top`, `bottom`, `left`, `right`. Hält diesen Bereich im Bildausschnitt sichtbar.                                                        |
| `fit`        | `cover` (Rückfallwert) füllt die Fläche und kann beschneiden; `contain` zeigt das gesamte Bild im gewählten Seitenverhältnis.                                 |
| `zoom`       | Wahrheitswert oder `auto` (Standard). `true` aktiviert Zoom; `false` deaktiviert ihn; `auto` folgt der Website-Einstellung. Verwende `:zoom="true"` oder `:zoom="false"`.   |
| `appearance` | `quiet` oder `tint`. Ohne Angabe gilt die Website-Einstellung.                                                                                                |

Der Standard-Slot mit Markdown ergänzt Inhalt zwischen Bild und Bildunterschrift. Die Markdown-Policy akzeptiert `bleed` als Text: `"true"` erweitert das Bild ebenfalls; andere Textwerte behalten die normale Breite. Verwende vorzugsweise `outside`.

## quiz und quiz-question

Lass Leserinnen und Leser ihr Wissen prüfen. Wähle eine Antwort, prüfe sie und öffne anschließend das Ergebnis oder versuche es erneut.

<Quiz title="Entwurf prüfen" description="Probiere zwei Fragen aus.">
<QuizQuestion question="Was hilft einer Person, ein Bild zu verstehen, das sie nicht sehen kann?" type="single" :options='[{"text":"Beschreibender Alternativtext","correct":true},{"text":"Ein längerer Dateiname","correct":false}]'>
Alternativtext beschreibt das Bild für Menschen, die einen Screenreader verwenden.
</QuizQuestion>

<QuizQuestion question="Was solltest du vor dem Veröffentlichen prüfen?" type="multiple" :options='[{"text":"Links","correct":true},{"text":"Termine","correct":true},{"text":"Die Lieblingsfarbe der Redaktion","correct":false}]'>
Prüfe, ob die Links funktionieren und die Termine stimmen.
</QuizQuestion>
</Quiz>

**In deine Markdown-Datei kopieren:**

```mdc [content/docs/component-example.md]
<Quiz title="Entwurf prüfen" description="Probiere zwei Fragen aus.">
<QuizQuestion question="Was hilft einer Person, ein Bild zu verstehen, das sie nicht sehen kann?" type="single" :options='[{"text":"Beschreibender Alternativtext","correct":true},{"text":"Ein längerer Dateiname","correct":false}]'>
Alternativtext beschreibt das Bild für Menschen, die einen Screenreader verwenden.
</QuizQuestion>

<QuizQuestion question="Was solltest du vor dem Veröffentlichen prüfen?" type="multiple" :options='[{"text":"Links","correct":true},{"text":"Termine","correct":true},{"text":"Die Lieblingsfarbe der Redaktion","correct":false}]'>
Prüfe, ob die Links funktionieren und die Termine stimmen.
</QuizQuestion>
</Quiz>
```

| Eigenschaft                 | Werte, Standard und Wirkung                                                                                                                 |
| --------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------- |
| `quiz.title`                | Überschrift. Standardmäßig kein Titel.                                                                                                      |
| `quiz.description`          | Einleitungstext. Standardmäßig keine Beschreibung.                                                                                          |
| `quiz.appearance`           | `quiet` oder `tint`. Ohne Angabe gilt die Website-Einstellung.                                                                              |
| `quiz-question.question`    | **Erforderlich.** Fragetext.                                                                                                                |
| `quiz-question.type`        | `single` (Standard) oder `multiple`.                                                                                                        |
| `quiz-question.options`     | **Erforderlich.** JSON-Liste mit mindestens zwei Antworten, jeweils mit eindeutigem, nicht leerem `text` und einem Wahrheitswert `correct`. |
| `quiz-question.explanation` | Text nach dem Prüfen. Ohne Angabe wird der Standard-Slot mit Markdown verwendet.                                                            |

Setze mindestens zwei `quiz-question`-Kinder in `quiz`, damit Navigation, Ergebnis und Wiederholen erscheinen. Verwende `quiz-question` einzeln für eine einzelne Frage mit einem Schalter zum Wiederholen. Bei `single` muss genau eine Antwort `correct: true` haben. Bei `multiple` muss mindestens eine richtig sein; alle richtigen und keine falschen Antworten müssen ausgewählt werden. Ungültige Antworten stoppen die Darstellung mit einer Fehlermeldung.

Alle Beschriftungen der Bedienelemente sind optionale Texteigenschaften. Ohne Angabe folgen sie der Seitensprache. Das sind die eingebauten englischen und deutschen Standardwerte:

| Komponente      | Eigenschaft           | Englischer Standard          | Deutscher Standard                 |
| --------------- | --------------------- | ---------------------------- | ---------------------------------- |
| `quiz`          | `questionLabel`       | Question                     | Frage                              |
| `quiz`          | `backLabel`           | Back                         | Zurück                             |
| `quiz`          | `nextLabel`           | Next                         | Weiter                             |
| `quiz`          | `resultsLabel`        | See Results                  | Ergebnis anzeigen                  |
| `quiz`          | `correctSummaryLabel` | correct                      | richtig                            |
| `quiz`          | `perfectLabel`        | Perfect score!               | Alles richtig.                     |
| `quiz`          | `retryPrompt`         | Keep learning and try again. | Weiterlernen und erneut versuchen. |
| `quiz`          | `resetLabel`          | Try Again                    | Erneut versuchen                   |
| `quiz-question` | `checkLabel`          | Check Answer                 | Antwort pruefen                    |
| `quiz-question` | `correctLabel`        | Correct                      | Richtig                            |
| `quiz-question` | `incorrectLabel`      | Incorrect                    | Nicht richtig                      |
| `quiz-question` | `resetLabel`          | Try Again                    | Erneut versuchen                   |
| `quiz-question` | `multipleChoiceLabel` | (select all that apply)      | (mehrere Antworten möglich)        |

Setze eigene Beschriftungen auf die zugehörige Komponente, etwa `<QuizQuestion checkLabel="Meine Antwort prüfen">`. Das `resetLabel` einer Frage wird nur außerhalb eines `quiz` verwendet; innerhalb setzt der Quiz-Schalter alle Fragen zurück. Die Antworten dienen als Übung, nicht als gespeicherte Bewertung.

## toc

Füge ein aufklappbares Inhaltsverzeichnis für diese Seite ein. Öffne es, um zu einer Komponente zu springen.

<Toc title="Komponente finden" :depth="2" :open="false" />

**In deine Markdown-Datei kopieren:**

```mdc [content/docs/component-example.md]
<Toc title="Komponente finden" :depth="2" :open="false" />
```

| Eigenschaft | Werte, Standard und Wirkung                                                                                                                                      |
| ----------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `title`     | Text. Standard: die übersetzte Inhaltsverzeichnis-Beschriftung der Website.                                                                                      |
| `depth`     | Zahl. Behält Überschriften bis zu dieser Ebene; `2` bedeutet Überschriften zweiter Ebene. Standard: alle vom Seiteninhaltsverzeichnis gelieferten Überschriften. |
| `open`      | Wahrheitswert. Standard: `false`. Legt fest, ob das Verzeichnis anfangs geöffnet ist.                                                                            |

Keine Slots und keine Eigenschaft `appearance`. Die Komponente filtert das vorhandene Seiteninhaltsverzeichnis; sie ergänzt keine Überschriftenebenen, die die Seitenkonfiguration ausschließt. Ohne gelieferte Überschriften erscheint nichts.

## Normales Markdown und eigene Komponenten

Absätze, Überschriften, **fetter Text**, *Hervorhebungen*, Listen, Links, Tabellen, Zitate, Trennlinien und Codeblöcke benötigen keinen MDC-Tag. Ginko Docs gestaltet sie automatisch. Zum Beispiel:

```md [content/docs/article.md]
## Vor dem Veröffentlichen

Prüfe **Namen** und *Termine*.

- Lies den Text.
- Öffne jeden Link.

> Mache die nächste Handlung deutlich.

| Aufgabe | Zuständig |
| --- | --- |
| Durchsicht | Redaktionsteam |

> [!NOTE]
> Speichere deinen Entwurf, bevor du den Editor schließt.
```

Hinweise im GitHub-Stil unterstützen `NOTE`, `TIP`, `IMPORTANT`, `WARNING` und `CAUTION`. Verwende eine der oben gezeigten Hinweiskomponenten, wenn du Titel, Icon oder Darstellung selbst festlegen möchtest.

Diese Seite beschreibt die eingebauten Komponenten für die Inhaltserstellung. Website-spezifische Tags benötigen eine eigene Registrierung; sie sind nicht auf jeder Ginko-Docs-Website verfügbar. Beispielsweise gehört `release-status` zum [Beispiel für eigene Komponenten]($docs/customization/custom-components) dieser Dokumentations-App. Bitte die Website-Entwicklung, eine neue Komponente zu registrieren, bevor du einen eigenen Tag verwendest.
