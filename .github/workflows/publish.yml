name: Publish

on:
  workflow_dispatch:
    inputs:
      version:
        description: Exact package version from layer/package.json
        required: true
        type: string

permissions: {}

concurrency:
  group: publish-ginko-docs
  cancel-in-progress: false

jobs:
  verify:
    name: Verify certified artifact
    runs-on: ubuntu-latest
    timeout-minutes: 5
    permissions:
      actions: read
      contents: read

    steps:
      - name: Require main
        env:
          RELEASE_VERSION: ${{ inputs.version }}
        run: |
          test "$GITHUB_REF" = "refs/heads/main"
          node -e "if (!/^\\d+\\.\\d+\\.\\d+(?:-[0-9A-Za-z.-]+)?$/.test(process.env.RELEASE_VERSION)) throw new Error('Invalid release version')"

      - name: Find successful CI for this commit
        id: ci
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          run_id="$(gh api "repos/$GITHUB_REPOSITORY/actions/workflows/ci.yml/runs?head_sha=$GITHUB_SHA&event=push&status=completed&per_page=20" --jq '.workflow_runs | map(select(.conclusion == "success" and .head_branch == "main")) | first | .id // empty')"
          test -n "$run_id"
          printf 'run-id=%s\n' "$run_id" >> "$GITHUB_OUTPUT"

      - name: Require an unused release tag
        env:
          GH_TOKEN: ${{ github.token }}
          RELEASE_VERSION: ${{ inputs.version }}
        run: |
          STATUS=$(curl --silent --show-error --output /dev/null --write-out "%{http_code}" \
            --header "Authorization: Bearer $GH_TOKEN" \
            --header "Accept: application/vnd.github+json" \
            "$GITHUB_API_URL/repos/$GITHUB_REPOSITORY/git/ref/tags/v$RELEASE_VERSION")
          case "$STATUS" in
            404) ;;
            200) echo "Tag v$RELEASE_VERSION already exists" >&2; exit 1 ;;
            *) echo "Could not verify tag availability (HTTP $STATUS)" >&2; exit 1 ;;
          esac

      - uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803 # v6
        with:
          persist-credentials: false

      - uses: actions/download-artifact@37930b1c2abaa49bbe596cd826c3c89aef350131 # v7
        with:
          name: ginko-docs-release
          path: .release
          github-token: ${{ github.token }}
          run-id: ${{ steps.ci.outputs.run-id }}

      - name: Verify exact artifact
        env:
          RELEASE_VERSION: ${{ inputs.version }}
        run: >-
          node -e "const fs=require('node:fs');const crypto=require('node:crypto');const path=require('node:path');
          const files=fs.readdirSync('.release');const tarballs=files.filter(file=>file.endsWith('.tgz'));
          if(tarballs.length!==1||!files.includes('release-artifact.json')||!files.includes('release-certification.json'))throw new Error('Expected one tarball and both release manifests');
          const m=require('./.release/release-artifact.json');const c=require('./.release/release-certification.json');const p=require('./layer/package.json');const tarball=tarballs[0];
          if(!/^[a-z0-9._-]+\.tgz$/.test(tarball))throw new Error('Unsafe tarball filename');
          const digest=crypto.createHash('sha256').update(fs.readFileSync(path.join('.release',tarball))).digest('hex');
          const lanes=[...(c.lanes||[])].sort().join(',');
          const contentVersion=p.peerDependencies['@lupinum/ginko-content'].match(/>=([^ ]+)/)?.[1];
          if(m.packageName!=='@lupinum/ginko-docs'||m.packageVersion!==process.env.RELEASE_VERSION||m.commit!==process.env.GITHUB_SHA||!m.sourceClean||m.tarball!==tarball||m.sha256!==digest||c.packageName!==m.packageName||c.packageVersion!==m.packageVersion||c.commit!==m.commit||c.tarball!==tarball||c.sha256!==digest||c.contentVersion!==contentVersion||c.contentSource!=='registry'||!c.releaseEvidence||lanes!=='i18n-dropdown,i18n-list,single-tabs')throw new Error('Artifact failed exact-SHA release validation')"

      - name: Extract release notes
        env:
          RELEASE_VERSION: ${{ inputs.version }}
        run: |
          awk -v version="v$RELEASE_VERSION" '$0 == "## " version { capture=1 } capture && /^## / && $0 != "## " version { exit } capture' CHANGELOG.md > .release/release-notes.md
          test -s .release/release-notes.md

      - uses: actions/upload-artifact@b7c566a772e6b6bfb58ed0dc250532a479d7789f # v6
        with:
          name: verified-ginko-docs-release
          path: .release
          include-hidden-files: true
          if-no-files-found: error
          retention-days: 2

  publish:
    name: Publish to npm
    needs: verify
    runs-on: ubuntu-latest
    timeout-minutes: 5
    environment: npm
    permissions:
      actions: read
      contents: read
      id-token: write

    steps:
      - uses: actions/setup-node@249970729cb0ef3589644e2896645e5dc5ba9c38 # v6
        with:
          node-version: 24.11.0
          registry-url: https://registry.npmjs.org

      - uses: actions/download-artifact@37930b1c2abaa49bbe596cd826c3c89aef350131 # v7
        with:
          name: verified-ginko-docs-release
          path: .release

      - name: Publish certified tarball
        env:
          RELEASE_VERSION: ${{ inputs.version }}
        run: |
          node -e "const {spawnSync}=require('node:child_process');const [major,minor]=spawnSync('npm',['--version'],{encoding:'utf8'}).stdout.trim().split('.').map(Number);if(major<11||(major===11&&minor<5))throw new Error('npm 11.5.1 or newer is required')"
          TARBALL=$(node -e "process.stdout.write(require('./.release/release-artifact.json').tarball)")
          case "$RELEASE_VERSION" in *-*) NPM_TAG=next ;; *) NPM_TAG=latest ;; esac
          npm publish ".release/$TARBALL" --access public --tag "$NPM_TAG" --ignore-scripts --provenance

  github-release:
    name: Create GitHub release
    needs: publish
    runs-on: ubuntu-latest
    timeout-minutes: 5
    permissions:
      actions: read
      contents: write

    steps:
      - uses: actions/download-artifact@37930b1c2abaa49bbe596cd826c3c89aef350131 # v7
        with:
          name: verified-ginko-docs-release
          path: .release

      - name: Create release from the published artifact
        env:
          GH_TOKEN: ${{ github.token }}
          RELEASE_VERSION: ${{ inputs.version }}
        run: |
          TARBALL=$(node -e "process.stdout.write(require('./.release/release-artifact.json').tarball)")
          PRERELEASE=()
          case "$RELEASE_VERSION" in *-*) PRERELEASE=(--prerelease) ;; esac
          gh release create "v$RELEASE_VERSION" ".release/$TARBALL" \
            --repo "$GITHUB_REPOSITORY" \
            --target "$GITHUB_SHA" \
            --title "v$RELEASE_VERSION" \
            --notes-file .release/release-notes.md \
            "${PRERELEASE[@]}"
