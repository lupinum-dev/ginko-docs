---
title: Inhalte rendern
description: Rendere Ginko-Content-Dokumente mit den Prose- und Komponenten-Tags des Starters.
---

Ginko Content parst Markdown zu einem AST, und `<ContentRenderer>` macht daraus Vue-Ausgabe.

## Renderer-Vertrag

Der Renderer erhält ein vollständiges Dokument:

```vue
<ContentRenderer :value="page" />
```

Dieser Vertrag hält Seitenmetadaten, Body, Inhaltsverzeichnis und Komponenten-Kontext zusammen.

## Komponentenkatalog

Ginko Docs liefert die generischen Prose- und MDC-Komponenten für Dokumentationsseiten. Consumer ergänzen nur projektspezifische Komponenten, ordnen ihre Comark-Tags über `content.markdown.tags` zu und deklarieren für jeden neuen Tag den eng gefassten statischen Prop- und Slot-Vertrag über `content.componentPolicy` in `nuxt.config.ts`.

## Was zu prüfen ist

Generierte Docs-Seiten sollen Überschriften, Absätze, Code, Tabellen, Cards, Callouts und verschachtelte Komponenten ohne Hydration-Fehler rendern.
